{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build .",
    "clean": "rm -rf ./dist/*",
    "clean:build": "npm run clean && npm run build",
    "start": "node --expose-gc .",
    "monitor": "nodemon ."
  },
  "author": "FAR",
  "license": "MIT",
  "engines": {
    "npm": ">=10.2.3",
    "node": ">=18.20.4"
  },
  "dependencies": {
    "streams-logger": "file:.."
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^20.6.2",
    "typescript": "^5.1.6",
    "nodemon": "^3.0.1"
  }
}
